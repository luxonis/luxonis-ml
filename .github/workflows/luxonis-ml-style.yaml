name: pre-commit

on:
  push:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: luxonis/luxonis-ml/.github/actions/luxonis-ml-style@feature/custom-pre-commit-hooks
